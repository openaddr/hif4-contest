# 赛方红线合规自检 — 2026-08-25（2026-08-28 结案）

## 【2026-08-28 结案】规则变更：任务书不再限制 A@W 反推（用户确认）
- 红线撤销 → gw/gwf/s 全部机制在决赛审查下 100% 安全；问询文本作废；最严读法回退预案作废。
- **判例的定量教训**（违规包 25268.59411 ≈ 我方 25268，+0.002%）：拥有完整 W 也几乎无增量——因为 gw/gwf 已是评分目标的充分统计量（J = 精确逐调用输出误差到常数项）。信息不是剩余误差的瓶颈，离散搜索质量才是 → 开启 pairflip 探索（搜索类，规则无关）。
- 注意力侧与红线无关（无 W 概念）；exact-P 死于子进程隔离（规则变更不影响）。


## 【2026-08-28 增补】赛方审查判例（用户提供的审计记录 2026/8/27-8/28）

两个被判违规的真实方案（分数 25268.59411，代码证据非本仓库——我们无 _apply_linear_retarget/teacher_t）：

| | 判例一 retarget_blocks | 判例二 teacher_t | 我们 (v42) |
|---|---|---|---|
| state 内容 | WᵀQ(W)·(Q(W)ᵀQ(W))⁻¹（含 Wᵀ@Wq 交叉 Gram） | Q(W)+Q(W−Q(W)) 残差编码（input×output 全尺寸，重构原始 W） | C×C Gram（gw/gwf）+ C 维 s——不可还原 W |
| 消费端 | A@retarget_blocks ≈ A@W（bmm） | activation @ teacher_t ≈ A@W | x@gwf → C 维向量 → 标量目标 |
| 本质 | 动态期近似计算被禁乘积 | 动态期重建 W 并直接算 A@W | 标准 PTQ Hessian 携带（GPTQ 家族） |

**风险上调**：判例一的违规证据措辞点名"Wᵀ@Wq 交叉 Gram"的存储本身——我们的 gw=Q(W)ᵀQ(W)/gwf=WᵀQ(校准A) 同族（差异在消费方式：他们乘出输出，我们只做标量目标）。**向赛方确认口径升级为紧急事项**；若最严读法成立（state 禁一切 W 衍生信息），gw/gwf/s 均需撤（≈-1100 分），回退目标 v27 时代管线。

**待用户确认**：① 25268.59411 是否即我方分数的小数形式（若是，存在被抄袭/混淆风险）；② GitHub 仓库可见性——若 public 立即转 private。

（以下为 2026-08-25 原始自检，结论在字面读法下仍成立：动态路径从不计算 A@W。）


## 红线原文
"不允许以任何形式计算出A@W,并利用A@W进行你和反推出 Q(A)"
（用户转述；按"利用 A@W 进行优化/拟合和反推出 Q(A)"理解）

## 机械审计（example/solution/solution.py @ v42）

### 动态路径（产生 Q(A) 的 API 2/4/5/6）——红线直接管辖范围

| 计算位置 | 内容 | 判定 |
|---|---|---|
| `M = v4d @ gw − x @ gwf`（线性动态精化） | x（当前激活，本就合法持有）与两个 **C×C** 状态矩阵的乘积；**从不形成 N 维输出空间乘积，从不计算 A@W**；state 无法还原 W（C×C → N×C 欠定） | 字面读法：**合规** |
| `xs = x * s`（平滑） | 逐通道缩放，s 是校准期拟合的 C 维向量 | 见模糊项③ |
| 注意力动态（H/qs/u 全部来自校准 q/k 输入 Gram） | 注意力 API 无 W 概念 | **合规（任何读法）** |

### 校准路径（API 1 同时合法持有 weight 与 calib 激活）

| 计算位置 | 内容 | 判定 |
|---|---|---|
| `_refine_weight_values` 内 `xh @ w_final.T`（holdout 评分） | 校准期 A@W 形态乘积，只用于改进 **Q(W)** | 字面读法合规（不产生 Q(A)） |
| smooth 守门 `_joint_proxy`/`_proxy_one`：`xf @ wsub.T` | 校准期子采样 A@W，只用于接受/拒绝 s | 同上 |
| 基线 alpha 搜索 `a_big @ w_rows.T` | 同上 | 同上 |
| mode/旋转守门 `xt @ qq.T` 等 | 同上 | 同上 |
| `gwf = w_final.T @ q_used`（校准期计算，结果入 state） | W×校准A 混合 Gram | 见模糊项③ |

### state 逐字段溯源

| 字段 | 来源 | 含 W 信息？ |
|---|---|---|
| `s` | 校准激活能量 + **W 列能量**拟合（SmoothQuant 家族） | 是（间接，C 个标量） |
| `mode`/`order`/`u_act`/`gw` | 纯校准激活统计 | 否 |
| `gwf` | W × 校准A | 是 |
| `tmax` | 形状常数 | 否 |
| 注意力 `H`/`qs`/`rf`/`u`/`rot`/`gq` | 纯校准 q/k 输入 | 否 |

## 结论

1. **字面读法**（禁止"计算测试期 A@W 并用于反推 Q(A)"）：**完全合规**——动态期从未计算 A@W，也不可能。
2. **严格读法**（禁止动态期存在任何 W 衍生信息）：`gwf`（动态精化目标）与 `s`（平滑）会被判违规 → 影响约 -800/-207 分，需立即知悉。
3. **建议向赛方确认的问题（可直接复制发送）**：
   > 想确认红线的适用口径：我们的校准阶段会从 weight 与校准激活中提取压缩统计量（如 C×C 的 Gram 矩阵、每通道平滑系数，属于 GPTQ/SmoothQuant 类标准后训练量化做法），并通过 activation_state 携带，用于动态量化时的误差最小化目标。动态量化阶段本身不持有 weight、也从不计算 A@W 或任何输出空间乘积。请问这种"校准期 W 统计量入 state、动态期用作目标函数"的做法是否允许？还是红线意味着动态阶段不得使用任何 W 衍生信息？
4. 历史佐证：gw/gwf 自 v16 起已随 25+ 次提交被正常评分，平台侧未见自动拦截；风险主要在决赛人工代码审查，故需提前确认口径。
